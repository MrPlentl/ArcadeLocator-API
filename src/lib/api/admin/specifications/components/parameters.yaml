Validate:
    Action:
        - in: path
          name: action
          required: true
          description: Action to validate
          schema:
              type: string
              default: access
              enum:
                  - access
                  - test
